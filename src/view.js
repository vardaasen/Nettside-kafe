

function updateView() {
    document.getElementById("app") = /*HTML*/ `
        ${getHtmlPage()}
    `;
}

function getHtmlPage() {
    if (model.app.currentPage === "cafeMenu") {
        return 
    }
}
